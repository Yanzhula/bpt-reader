import React from "react";
import Alert from "react-bootstrap/Alert";

export const End: React.FC = () => {
  return (
    <Alert variant="secondary" style={{ width: "18rem", margin: "1em auto" }}>
      ⏹ End
    </Alert>
  );
};
